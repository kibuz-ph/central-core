model CategoryUser {
  id   String            @id @default(uuid())
  name UserApartmentType

  userApartments UserApartment[]

  @@map("category_users")
}

enum UserApartmentType {
  OWNER
  TENANT
  RESIDENT
}
