model Role {
  id        String          @id @default(uuid())
  name      user_role_types @unique
  userRoles UserRole[]

  @@unique([id])
  @@map("roles")
}

enum user_role_types {
  SUPER_ADMIN
  ADMIN
  USER
}
